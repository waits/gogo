{{define "title"}}{{if eq .Last "f"}}Game Over - {{end}}{{.Black}} vs. {{.White}} - Go{{end}}

{{define "content"}}
<h1 id="title">Turn <span id="turn">{{.Turn}}{{if eq .Last "f"}}/F{{end}}</span> – {{.Black}} vs. {{.White}}</h1>

{{if ne .Last "f"}}
  <p>
    <form>
      <strong>Player</strong>
      <label><input type="radio" id="color_1" name="color" value="1" data-color="black" /> {{.Black}} (black)</label>
      <label><input type="radio" id="color_2" name="color" value="2" data-color="white" /> {{.White}} (white)</label>
    </form>
  </p>
{{end}}

<p><strong>Captured:</strong> black <span id="blackscr">{{.BlackScr}}</span>, white <span id="whitescr">{{.WhiteScr}}</span></p>
{{if ne .Last "f"}}<p><button id="pass">Pass</button></p>{{end}}

<div id="board" class="inactive">
  {{range $y, $row := .Board}}
  <div class="row">
    {{range $x, $piece := $row}}
    <div class="cell">
      <svg width="30" height="30" viewPort="0 0 30 30" version="1.1" xmlns="http://www.w3.org/2000/svg">
        <line x1="{{if eq $x 0}}15{{else}}0{{end}}" y1="15" x2="{{if eq $x .ZeroSize}}15{{else}}30{{end}}" y2="15" stroke="black" stroke-width="1" />
        <line x1="15" y1="{{if eq $y 0}}15{{else}}0{{end}}" x2="15" y2="{{if eq $y .ZeroSize}}15{{else}}30{{end}}" stroke="black" stroke-width="1" />
      </svg>
      {{if eq $piece 1}}<div class="piece black"></div>{{else if eq $piece 2}}<div class="piece white"></div>{{else}}<div class="piece hide"></div>{{end}}
    </div>
    {{end}}
  </div>
  {{end}}
</div>
{{end}}

{{define "script"}}
{{if ne .Last "f"}}
  <script>
    'use strict';
    var board = document.getElementById('board');
    var passBtn = document.getElementById('pass');
    board && new GameController(board, passBtn, {{.Key}}, {{.Black}}, {{.White}});
  </script>
{{end}}
{{end}}
